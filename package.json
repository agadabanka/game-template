{
  "name": "game-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A clean, maintained base game to instantiate new games from (the engine + tooling, neutral placeholder skin).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "gen:image": "node scripts/gen-image.mjs",
    "analyze:image": "node scripts/analyze-image.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "express": "^4.21.2",
    "google-auth-library": "^9.15.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.56.1"
  }
}
